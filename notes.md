## goal is to run linux commands on powershell

- create a script for powershell
- it should support all commonly used linux commands such as 
    - ls -alGF
    - | grep <text>  (pipe to grep)
    - ps -aux
    - find with arguments


- explain where copy the script so it is loaded by default by powershell, much like .zshrc for zsh

- write summary to summary.md
